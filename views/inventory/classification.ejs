<!-- three distinct sections: A content heading, a message, and the vehicles display. -->

<!-- Content Heading: show the title, else return tp home screen -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- The Message Block: Ex if an error appears -->
<%# messages() %>

<!-- The Vehicles Display -->
<%- grid %>